# 第三轮两项改进试验：学习率增补候选清单

日期：2026-09-26。本清单在初始 A/B 四项内部选择已经冻结后新增，因此不是初始候选清单的一部分。新增原因是 A 的 6 个起点在 62 至 199 次整批更新时依旧因“连续 5 次没有至少改善 0.0001”停止，无法从该结果分辨当前学习率 0.001 是否最适合。

本次只新增 A 的基础日表 LSTM 和 GRU 学习率比较：0.0005 与 0.002。已有 0.001 的 270 次上限结果是第三个参照。每个学习率和模型使用 3 个固定随机起点，最多 270 次整批更新；16 个隐藏单元、AdamW、参数限制系数 0.0001、梯度上限 1.0、整批 525 户更新和旧提前停止规则全部不变。新增 2 个学习率 × 2 个模型 × 3 个起点，共最多 12 次训练。

每个模型只在自己的三个学习率中，按 3 个起点“旧阈值保存的较晚日期检查误差”的平均值选最小者。LSTM 和 GRU 不互相淘汰。三起点逐题中位数的合并内部误差会另存，不能替换主选择规则。B 的 12/13 字段比较不改变学习率或更新次数。

初始四候选曾在全部四项冻结后完成过一次 113 户范围确定和 142 户开发评分，共 1,704 条逐题期限记录；该评分在学习率增补计划到达前发生，没有用于增补学习率选择。更早一次因候选尚未全冻结而中止的评分目录也保留为过程记录。因为这两次已发生的评分，本任务最后的评分不能写成“全任务只评分一次”；最终冻结后的评分将单独保存，且不用于再选参数。

本次学习率候选完成后停止搜索：不再提高更新上限，不调整隐藏单元、优化器、权重限制或输入资料；即使某个 0.0005 候选到 270 次仍下降，也只记录“本上限未收敛”，不继续训练。最终所有候选冻结后，才各选用版本一次性使用 113 户确定范围、使用 142 户评分。
